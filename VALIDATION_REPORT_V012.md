# mint-report-html v0.12 候选验收报告

日期：2026-09-02

## 结论

v0.12 已通过内容合同、全类型编辑、单工作文件、版本血缘、离线移交、确定性合并、浏览器交互、视觉几何、当前状态 PDF、原生可编辑 PPTX 和性能回归，并已更新到 `/Users/mac/.codex/skills/mint-report-html`。`mint-report-deck` 未进入修改范围。

30 分钟口径仍只适用于方案定义的标准边界。自动测试可以证明 Skill 的确定性机械流水线留有充足余量，不能替代真实模型排队、负责人材料质量和人工审阅时间；正式使用仍应先做一次真实汇报演练。

## 已验证结果

| 门禁 | 结果 |
|---|---|
| 静态/合同 | 13 组通过；Skill 快速校验通过 |
| 全盘编辑 | 标题、正文、表格、图表、图片、关系图全部编辑并写回内嵌模型 |
| 保存 | 直接保存逻辑通过；无文件写入 API 时只下载一个当前版 HTML |
| 离线移交 | 文件改名并复制到空目录后，图片、编辑和保存正常 |
| 合并 | 文件名无关；重复哈希去重；严格后代选新；分叉阻断；合并模型调用数为 0 |
| 结构 revision | 保留领导已改标题/正文，同时刷新最新交互和来源结构 |
| 交互 | 路径、聚焦、引导、离线、键盘、编辑态和当前 PDF 全部通过 |
| 增量 | 普通 revision 只编译受影响 Scene；失败候选不覆盖 last-good |
| 发布 | 同一浏览器快照生成 PDF，并供 PPTX 布局复用；不重复启动导出浏览器 |
| 合并后领导修改 | 从领导最后保存的完整工作 HTML 恢复后，修改后的表格和关系图进入最终 PPTX/PDF |
| PPTX | 16:9、无页眉页脚页码、原生可编辑对象；`slides_test.py` 无溢出 |
| 60MB 素材 | 单 HTML 80.1MB；Chrome 打开 593ms、生成保存内容 566ms；可编辑、可解包 |
| 12 Scene 连续三次 | 3 人、8 项大纲、12 Scene；12.274s、9.778s、10.791s；三次均通过 HTML/PDF/PPTX 门禁 |

12 Scene 数据是确定性 Skill 流水线基准，`modelCallsByScripts=0`；不把它冒充 Agent 内容创作和人工审阅耗时。

## 浏览器边界

本机 Chrome 152 的真实 `file://` 环境返回 `isSecureContext=true`，并暴露 `showSaveFilePicker`。自动化已验证写入接口集成和回退下载；系统原生选择器、用户授权及关闭重开后的权限恢复由浏览器控制。详见 `references/browser-support.md`。

## 回滚

实施前完整候选仓库备份：`/Users/mac/Documents/Mint/mint-report-html.backup-20260902-pre-v012`。原已安装版本备份：`/Users/mac/.codex/skills/mint-report-html.backup-20260902-120916`。安装后已从已安装路径重跑关键合同、全类型编辑/保存/合并/发布浏览器测试和单浏览器快照测试。

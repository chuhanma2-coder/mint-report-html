# mint-report-html v0.12 完整优化方案（评审稿）

> 状态：已批准并进入 v0.12 候选实施；实测证据见 `VALIDATION_REPORT_V012.md`。
>
> 实施边界：只修改 `mint-report-html`，不修改 `mint-report-deck`，不新增与本次问题无关的产品功能。
>
> 发布原则：不是“尽量快”，而是把速度写成上线门禁。标准用例未达到 30 分钟、或任一内容/编辑/合并硬门下降，v0.12 就不替换当前可用版本。

## 1. 结论先行

当前 v0.11 已经具备来源锁、完整数据表达路由、HTML 全类型编辑、ZIP 完整性、确定性合并、最终原生可编辑 PPTX/PDF 等基础能力。v0.12 不推翻这些能力，而是解决实际协作时仍然过重的用户操作：

1. 任务卡由一名协调人单独生成一次，避免多人各自生成不同任务卡；Skill 自动写入版本和设计契约指纹，协调人不用制定视觉规则。
2. 每位负责人日常只接触一个 `.mint-section.html` 工作文件；ZIP 仍从第一版开始存在，但内嵌在 HTML 中，不要求用户反复导出、改名和发送 ZIP。
3. 领导可以随到随审，直接改标题、正文、表格、图表数据、图片和关系图；保存后还是同一个工作文件。
4. 需要拆页、并页、换主题或重组论述时，把当前工作 HTML 交给 Agent；Agent 只重做受影响 Scene，不能重读全部来源、不能重写无关页面。
5. 合并器直接读取 A/B/C 当前 HTML 中的内嵌协作包，根据任务卡排序、校验、合并，不通过 DOM 反推内容，也不再次总结或美化。
6. 合并后的完整 HTML 再供领导审阅；最终只发布一次 HTML、原生可编辑 PPTX 和 PDF。
7. 16:9、简洁清新、无页眉/页脚/页码、完整离线素材、全盘编辑是 Skill 内置规则，不要求协调人或负责人每次写进 Prompt。

目标流程是：

```text
领导大纲 + 已确定分工
          ↓  协调人只执行一次
本次任务卡（身份、顺序、分工；不是视觉模板）
          ↓
A / B / C 并行生成各自一个可编辑工作 HTML
          ↓
领导按各自进度分别审阅、直接改或提意见
          ↓
各负责人保存同一工作 HTML；结构性修改只重做受影响 Scene
          ↓
一次确定性合并 → 完整可编辑 review.html
          ↓
领导整体验收 → 一次发布 HTML + PPTX + PDF
```

## 2. 本次优化要解决的问题与验收结果

| 领导问题 | v0.12 方案 | 可验收结果 |
|---|---|---|
| 多人结果不一致 | 共同任务卡固定身份、章节顺序、来源边界及自动版本指纹；视觉品牌、16:9、无页面装饰由 Skill 固定；完整报告使用同一设计契约 | 合并后无 ID、背景、字体、图片尺寸和顺序冲突 |
| 每页单独跑、速度慢 | 每位负责人整章一次生成，所有 Scene 批量创作；来源只规范化一次；修改只处理受影响 Scene | 不再按页开任务；标准用例端到端不超过 30 分钟 |
| 合并复杂 | 工作 HTML 内嵌标准协作包；合并器只做校验、命名空间和排序 | 用户不手工改页码、背景、图片和字体 |
| HTML 难以保存 | Chrome/Edge 首次授权后支持工作文件保存；不支持时降级为一次“保存当前版 HTML” | 修改不会只留在浏览器内存；发送的是包含修改的单文件 |
| 图片发给别人后消失 | 所有已引用图片、图标和必要字体内嵌；外链、绝对路径和项目外素材直接阻断交付 | 文件移动到空目录或另一台离线机器仍能显示全部图片 |
| 只有标题可编辑、编辑框过窄 | 正式可见内容 100% 绑定字段；文本与表格/图表/图片/关系图分别有编辑器；增加编辑态几何门禁 | 不写 Prompt 也能按 E 全盘编辑；编辑区不截断 |
| 修改后重新导 ZIP 很麻烦 | ZIP 变为 HTML 内部格式；日常只保存工作 HTML；需要技术排障时才显式导出 ZIP | 正常协作不出现“每改一次导一个 ZIP” |
| 内容被 AI 丢失、乱加 | 来源锁、内容去向台账、零无来源事实仍为硬门；合并禁止二次概括 | 所有来源单元都有去向，未知项明确标注而非补写 |
| 美观性 | 保留“先读内容和关系，再自由艺术指导”的机制；不引入固定六类页面或母版白名单 | 简洁清新但不模板化；不同内容可以采用不同构图 |
| 最终可发送、可修改 | 最终 HTML 单文件、PDF 和原生可编辑 PPTX 来自同一当前模型 | 三种格式内容、标题、数据、顺序一致；PPTX 非整页截图 |
| 页眉页脚页码 | 所有个人、合并和最终产物都不生成页面装饰 | 合并不需要重新编号；PPTX 仍无页码 |

## 3. 用户看到的完整流程

### 3.1 第一步：协调人只生成一次任务卡

团队照常按岗位和领导大纲认领工作。任务卡不是让团队重新分工，也不是让 Agent 决定谁负责什么；它是合并器使用的“本次报告身份证”。

协调人提供：

- 领导原始大纲，保持原编号和原文字；
- 已经确定的负责人范围；
- 汇报名称。

协调人示例 Prompt：

```text
使用 mint-report-html，为本次汇报建立协作任务卡。
不要制作页面，不要改写大纲，不要补充缺失编号。

汇报名称：2026 年 9 月经营例会
A 负责大纲 1、2、3；B 负责 4、6；C 负责 7、8。
领导大纲见附件。
```

Skill 输出一份小文件：

```json
{
  "reportId": "2026-09-operating-review",
  "title": "2026 年 9 月经营例会",
  "skillContractVersion": "mint-report-html/0.12",
  "designContract": {
    "tokenVersion": "mint-clean/1",
    "artDirectionSeed": "2026-09-operating-review",
    "stage": "1920x1080",
    "pageChrome": "none"
  },
  "outlineOrder": ["1", "2", "3", "4", "6", "7", "8"],
  "sections": [
    {"sectionId": "section-a-1-3", "owner": "A", "outlineItems": ["1", "2", "3"], "order": 1},
    {"sectionId": "section-b-4-6", "owner": "B", "outlineItems": ["4", "6"], "order": 2},
    {"sectionId": "section-c-7-8", "owner": "C", "outlineItems": ["7", "8"], "order": 3}
  ],
  "warnings": ["原始大纲未出现第 5 项，已原样保留，未自行补写"]
}
```

说明：

- `sectionId` 由 Skill 自动生成，用户不需要自己想，也不依赖文件名。
- `skillContractVersion` 和 `designContract` 由 Skill 自动写入，不是让协调人选择字体、颜色或版式；它们用于保证 A/B/C 使用同一能力和基础视觉锚点。
- 任务卡不重复写“简洁清新、16:9、无页眉页脚页码”等规则；这些是 Skill 永久内置要求。
- 任务卡不确定各负责人的具体页码。实际 Scene 数由内容决定，最终仍按任务卡章节顺序合并。
- 这一步必须由指定的一人做一次，不允许 A/B/C 各自生成任务卡。

### 3.2 第二步：A/B/C 并行制作自己的工作文件

每位负责人向自己的 Codex 提供：

- 同一份任务卡；
- 自己的原始材料；
- 自己在任务卡中的负责人名称或 `sectionId`。

负责人示例 Prompt：

```text
使用 mint-report-html 制作我负责的部分。
附件 1 是本次协作任务卡，我是 A；其余附件是我的原始材料。
只使用原始材料，不联网补写，不添加材料中没有的事实。
请先理解内容和数据关系，再选择合适表达；不要因为出现多个数字就机械画图。
输出一个可预览、按 E 全盘编辑、可保存和可合并的当前版 HTML 工作文件。
```

用户得到且日常只需要管理：

```text
A-当前版.mint-section.html
B-当前版.mint-section.html
C-当前版.mint-section.html
```

文件可以改名。身份来自文件内部的 `reportId + sectionId + revision + contentHash`，不来自文件名；因此 `A-final-真的最终版.html` 也可以正确识别。

### 3.3 第三步：领导按进度分别审阅

这一步适配已经确认的真实工作方式：每个人进度不同，做好一部分就先审一部分。

领导有三种修改方式：

| 修改类型 | 操作方式 | 是否需要 Agent |
|---|---|---|
| 改标题、正文措辞、表格单元格、图表数据/标题/单位/来源、替换图片、修改关系图标签 | 打开工作 HTML，按 `E`，直接修改并保存 | 不需要 |
| 对细节提出意见 | 负责人直接在 HTML 修改，或把当前 HTML 和意见交给 Agent | 可选 |
| 拆成多页、数页合一、调整主题、改变论证顺序或换整体构图 | 把当前工作 HTML 和意见交给 Agent，走结构性 revision | 需要 |

直接编辑必须更新内嵌统一模型，不能只改页面 DOM。结构性 revision 必须先读取当前工作 HTML 中已有的人工修改，再重做受影响 Scene，不能退回最初版本。

### 3.4 第四步：保存，而不是反复导 ZIP

目标交互：

1. 第一次保存时，Chrome/Edge 请求用户选择或确认当前工作文件。
2. 同一浏览器会话内，此后点击“保存”或按页面定义的 `Cmd/Ctrl+S`，写回同一文件。
3. 重新打开文件后若浏览器安全策略要求重新授权，只需再次选择该文件；不要求导出 ZIP。
4. 如果浏览器不支持直接写文件，显示明确的降级按钮“保存当前版 HTML”，只下载一个包含全部修改的新 HTML；不额外下载 ZIP。

浏览器直接写本地文件受安全模型约束，因此实施第一阶段必须先做技术验证。若验证不通过，不得把“同文件保存”写成已实现；正式支持矩阵将明确 Chrome/Edge，Safari 等浏览器使用降级保存。

### 3.5 第五步：合并当前工作文件

协调人把 A/B/C 当前版工作 HTML 一次性交给 Codex：

```text
使用 mint-report-html 合并本次汇报。
附件是本次任务卡和 A、B、C 当前版工作文件。
不得重新总结、改写或补充任何章节；按任务卡顺序合并并执行完整性检查。
先输出完整可编辑 review.html，暂不生成 PPTX/PDF。
```

合并器执行的是确定性程序，不调用模型改内容：

1. 从每个工作 HTML 读取内嵌标准包；
2. 校验报告身份、章节身份、来源锁、完整性哈希和修订谱系；
3. 对 Scene、字段、图片和样式 ID 做章节命名空间隔离；
4. 根据任务卡的章节顺序拼装；
5. 确定性统一设计 token、字体资源、舞台比例、背景基线和动效节奏；只改全局契约，不调用模型重写内容或把页面套入固定模板；
6. 保留每个负责人的人工修改、来源台账和设计结果；
7. 输出完整的可编辑 `review.html`。

不生成页码，因此不存在先删个人页码、再重排页码的问题。

### 3.6 第六步：完整审阅和最终发布

领导可以继续在完整 `review.html` 里直接编辑；如果要跨章节合并页面或调整总叙事，再执行一次结构性 revision。

确认后只执行一次最终发布：

```text
完整汇报.html
完整汇报.pptx
完整汇报.pdf
```

三种格式来自同一 `report-model`。HTML 的滚动、悬停、点击和动画在 PPTX 中使用清晰的静态状态表达，不承诺交互效果像素级一比一；但标题、正文、数据、图表类别/系列、图片、关系、场景顺序和来源必须一致。PPTX 中的文字、表格、图表、媒体、形状和关系图必须是原生可编辑对象，禁止整页截图。

## 4. 内部文件与数据架构

### 4.1 ZIP 到底在什么时候生成

不是“最后才从 HTML 反向生成 ZIP”。正确架构是：

```text
任务卡 + 原始来源
        ↓
统一来源锁 / 内容图 / report-model / Scene 源 / 资产
        ↓
标准章节包（从第一版就存在）
        ↓  内嵌
单文件工作 HTML
        ↓  编辑只更新统一模型和修订记录
合并器读取内嵌章节包
        ↓
完整统一模型
        ↓
HTML / 原生 PPTX / PDF
```

ZIP 是一种技术封装能力，不再是日常用户文件。技术排障、长期归档或兼容旧版本时可显式导出 `.mint-section.zip`，正常审阅和协作不需要。

### 4.2 工作 HTML 的内部组成

为兼顾速度和完整性，工作文件分为两层：

1. 不可变层：原始来源摘要块、来源锁、Scene 源、图片/字体等素材及其已计算哈希。
2. 可变层：`report-model`、用户修改、修订谱系、当前 Scene 顺序、任务卡引用和清单。

保存时只重新序列化可变层，复用不可变层和已计算哈希；移交或合并时再做一次全包完整性校验。这样不会在每次输入一个字时重算几十 MB 图片和全部来源。

### 4.3 身份、改名和修订谱系

每个工作文件内部至少包含：

```json
{
  "workfileVersion": "0.12",
  "reportId": "2026-09-operating-review",
  "sectionId": "section-a-1-3",
  "revision": 4,
  "parentContentHash": "上一版内容哈希",
  "contentHash": "当前内容哈希",
  "updatedAt": "ISO 时间",
  "updatedBy": "A 或领导"
}
```

因此不强制用户只能使用默认文件名。默认命名用于降低混乱，但不能把“禁止改名”当作系统正确性的前提。

同一章节出现多份文件时：

- 同 `reportId + sectionId + contentHash`：视为完全相同，自动去重。
- 一份是另一份严格祖先、修订链连续：默认使用后代版本，并在合并报告中说明。
- 同 revision 但 contentHash 不同：阻断合并，要求选择。
- 两份从共同祖先分叉：阻断合并，不猜哪份正确。
- 报告或章节身份不匹配：阻断合并。

## 5. 全盘编辑必须成为强制能力

### 5.1 不依赖用户 Prompt

无论用户是否写“要按 E 编辑”，Skill 都必须生成：

- 可见且可键盘操作的编辑入口；
- `E` 快捷键；
- 文本编辑；
- 表格、图表、图片/媒体、关系图的类型化编辑器；
- 保存状态、未保存提醒和修订记录。

标题可编辑而正文或图表不可编辑，直接判定候选失败。

### 5.2 编辑范围

| 内容 | 最低编辑能力 |
|---|---|
| 大标题/小标题/正文/注释/来源 | 直接改文字、换行和必要的显示长度提示 |
| 表格 | 表头、行列和所有单元格；支持粘贴 TSV |
| 图表 | 图表标题、类型、单位、期间、来源、分类、系列名称和数值 |
| 图片/媒体 | 替换素材、替代文本、标题/说明、裁切模式 |
| 流程/关系图 | 节点文字、节点增删、连接关系和关系标签 |
| Scene 结构 | 拆页、并页、主题和叙事顺序通过 Agent revision 完成 |

“全盘可编辑”指正式业务内容全部可改，不等于在 HTML 里提供 PowerPoint 式任意拖拽坐标。任意位置排版调整会显著增加复杂度并降低稳定性；最终 PPTX 可继续用 PowerPoint 做原生对象级排版调整。

### 5.3 编辑框过窄的专门修复

增加以下浏览器硬门：

- 进入编辑态后，文本编辑区域宽度不得小于其正常内容容器宽度的 95%；
- 长文本可完整滚动/换行，`scrollWidth`、`clientWidth` 和 overflow 不得造成不可到达内容；
- 光标必须能到达字段最后一个字符；
- 类型化编辑器在常用桌面尺寸下不得小于 `min(900px, 视口宽度 - 48px)`；
- 对标题、正文、表格、图表、图片和关系图各执行真实编辑、保存、重开和回读测试。

## 6. 数据关系与表达路由不能因提速而缩水

现有完整路由规则继续保留，不能简化成“出现多个连续数字就画图”，也不能只支持固定六类页面。Agent 必须先识别数据的语义关系，再选表达。

路由至少覆盖：

- 单值/KPI、多个互不相关指标、同单位比较、异单位比较；
- 排名、Top/Bottom、目标对实际、计划对进度、预算对实际、差额和完成率；
- 时间趋势、同比/环比、前后对比、阶段变化、增量/累计、预测区间；
- 构成占比、贡献拆解、瀑布增减、分布、区间、离群点；
- 漏斗、转化链路、流程、时间线、里程碑、甘特；
- 二维矩阵、象限、气泡关系、相关性与非因果提醒；
- 地理分布、流向、网络关系、层级、树状结构、桑基/流量；
- 风险矩阵、优先级、情景分析、敏感性、置信区间；
- 精确查数表、明细表、对照表，以及不应画图的定性内容；
- 单位、口径、基期、分母不一致时的阻断或拆开展示规则；
- 原 PPTX 已存在图表时，优先保留图表类型、分类、系列和值，除非内容关系明确要求调整。

### 路由规则如何既完整又不拖慢 Agent

完整规则保存在 `references/data-expression-routing.md`，但不要求模型每次把整份长文加载进上下文。实施一个确定性、零模型调用的路由选择器：

1. 内容编译阶段先产出关系标签，例如 `time-series`、`ranking`、`part-to-whole`、`funnel`。
2. 选择器从完整规则库取回对应的首选表达、备选表达和反规则。
3. Agent 只读取命中的几条规则并完成艺术指导。
4. 测试确保完整规则库的每一类都有可命中入口，未命中时不得默认“柱状图”。

这样减少上下文和推理时间，但没有删除任何表达能力，也不引入固定页面模板。

## 7. 速度与效果如何平衡（核心专项）

### 7.1 不用删能力换速度，而是消灭重复工作

以下硬门在个人草稿、个人修改、合并和最终发布各阶段都不能关闭：

- 来源锁和内容去向完整；
- 零无来源事实，不能联网补写；
- 数据关系正确，数字口径不冲突；
- 当前阶段正式内容 100% 可编辑；
- 工作 HTML 离线、自包含；
- 文件身份、修订谱系和完整性有效；
- 失败候选不能覆盖上一个可用版本。

真正耗时、但无需每人每次重复做的工作延后到最终发布一次完成：

- laptop/mobile/print 全视口视觉检查；
- PDF 当前状态检查；
- PPTX 原生对象导出和溢出检查；
- 全报告跨章节一致性和完整发布清单。

个人阶段仍执行静态内容门、来源门、编辑门、离线门和桌面当前/受影响 Scene 的浏览器检查。因此“快”不是跳过内容质量，而是把格式和全视口成本从 N 次降为 1 次。

### 7.2 五条执行路径

| 路径 | 做什么 | 不做什么 |
|---|---|---|
| `task-card` | 读取大纲和已定分工，确定性生成一次任务卡 | 不做内容页、不做视觉 QA |
| `section-draft` | 规范化一次来源、编译内容、批量生成整章 Scene、桌面 QA、输出工作 HTML | 不逐页开任务、不出 PPTX/PDF |
| `section-revision` | 读取当前工作文件，只重做受影响 Scene，执行增量门禁 | 不重读未变来源、不重做无关 Scene |
| `merge-review` | 读取内嵌包、完整性检查、命名空间、排序、完整 review HTML | 不让模型二次总结、不出 PPTX/PDF |
| `publish` | 完整多视口/打印 QA，一次生成 HTML/PPTX/PDF | 不再对各章节分别发布 |

### 7.3 Agent 生成阶段的提速措施

1. **整章批量生成**：一个负责人一次任务完成全部 Scene，不允许“每一页单独开会话”。
2. **一次中文内容编译**：来源规范化后只生成一次来源锁、内容图和管理问题聚类；禁止再调用模型做第二份摘要。
3. **来源缓存**：Office/PDF 按 `sourceHash` 规范化；未变化的文件永不重开。扫描件 OCR 明确走单独准备阶段。
4. **一次统一艺术方向**：同一章节在生成 Scene 前确定一次色彩、字体、留白、密度节奏和动态语言，所有 Scene 共享；不让每页独立想一套风格。
5. **按关系加载路由**：只给 Agent 命中的数据表达规则，不把完整长规则每次塞进上下文。
6. **渐进加载 Skill 文档**：`SKILL.md` 保持短入口；协作、PPTX、路由等细节只在对应阶段读取。能力仍在，但无关阶段不占上下文。
7. **结构预检**：超过建议 Scene 数时先快速确认管理问题和顺序，避免做完视觉再大拆大并。
8. **不生成无用中间格式**：个人阶段只生成工作 HTML；不做个人 PDF、PPTX、缩略图合集和技术 ZIP 下载。

### 7.4 浏览器、保存和打包阶段的提速措施

1. 当前模型若在每次按键都计算整包 SHA-256，改为 300–500ms 防抖或在 blur/save 时计算。
2. 不可变素材哈希只算一次；保存只重算可变模型和 manifest，合并/移交时再执行全包校验。
3. 修改只对受影响 Scene 做桌面浏览器 QA；无变化 revision 直接退出。
4. 最终发布复用一个浏览器会话完成 desktop/laptop/mobile/print 状态采集，避免视觉 QA、PDF 和 PPT 布局脚本分别冷启动浏览器。
5. 生成一次 `publish-snapshot`，PDF 和 PPTX 使用同一当前内容哈希和已验证几何快照，避免重复渲染同一状态。
6. 只内嵌实际引用素材。显示图使用适合当前展示尺寸的高质量版本；需要后续替换或 PPTX 原生编辑的素材保留必要原件，未引用素材不打包。

### 7.5 30 分钟不是口号，而是有边界的上线门禁

无法对无限文件、数百页扫描 OCR、网络故障或任意模型排队做无条件时间承诺。为真正“保证”，v0.12 定义一个领导下周汇报型标准用例，并在支持环境中用自动计时验收；不达标不上线。

**标准用例边界：**

- 1 份任务卡、3 名负责人、8 个大纲项；
- 合计 8–12 个 Scene；
- 每位负责人不超过 10 个普通 Office/PDF/文本来源，三人合计不超过 100 个有文字层的来源页；
- 无待处理扫描 OCR；若有扫描件，先完成 OCR 再进入 30 分钟计时；
- 每个章节实际引用素材不超过 20 张，内嵌资产不超过 60 MB；
- 不联网研究、不等待人工确认；
- 三位负责人分别在自己的 Codex 中并行执行；支持环境为本地 macOS + Chrome/Edge + 已安装的展示运行时。

**计时口径：** 从任务卡和原始材料准备好开始，到三个个人工作 HTML、一次合并、最终 HTML/PPTX/PDF 全部通过门禁为止；不包含领导人工审阅和同事之间等待时间。

**时间预算：**

| 阶段 | 上线目标 |
|---|---:|
| 任务卡 | ≤ 10 秒 |
| 单人首次工作 HTML（每人，并行） | ≤ 12 分钟 |
| 三人并行墙钟时间 | ≤ 12 分钟 |
| 一次普通个人 revision | ≤ 3 分钟 |
| 三章节确定性合并 | ≤ 30 秒 |
| 12 Scene 最终全 QA + HTML/PPTX/PDF | ≤ 3 分钟 |
| 编排、文件传递和安全缓冲 | 约 11 分 20 秒 |
| 标准用例端到端 | **≤ 30 分钟** |

现有证据仅表明“两页、来源已规范化后的最终 publish”为 16.9 秒，不能拿它冒充任意报告的承诺。v0.12 上线前必须对上述标准用例连续执行 3 次，3 次均不超过 30 分钟；任何一次超过，先定位瓶颈，不能通过放宽内容硬门来拿到通过。

如果输入超出标准边界，Skill 在 20 秒内完成容量预检，明确显示文件数、页数、扫描页、素材大小和预计 Scene 数，并说明需要预 OCR、拆批或增加并行负责人。它仍需保证内容不丢失，但不虚假承诺 30 分钟。

### 7.6 防止“提速后效果下降”的回归门禁

每次性能优化都必须同时比较 v0.11 基线：

- 来源覆盖率不得下降，必须保持 100%；
- 无来源事实必须为 0；
- 正式可见字段编辑覆盖率必须为 100%；
- 数据路由合同测试数量不得减少；
- 图表、表格、媒体、关系图的类型化编辑测试全部通过；
- 离线资源为 0 个外链/丢失路径；
- 最终 PPTX 仍是原生对象、16:9、无页眉页脚页码；
- 同一输入的任务卡、排序和合并结果必须确定；
- 视觉 QA 的碰撞、溢出、标题断行和打印检查不得放宽阈值。

如果速度达到但任一指标回退，版本判定失败。

## 8. 美观与一致性设计

一致性不等于固定模板。v0.12 继续采用两层机制：

### 永久固定的品牌和交付锚点

- 简洁、清新、专业；
- 全报告共享字体族、基础字号尺度、色彩 token、间距尺度和动效节奏；
- HTML 舞台和最终 PPTX 均以 16:9 视觉安全区为基准；
- 无页眉、页脚和页码；
- 标题层级和建议范围内置：1600×900 舞台中，汇报/章节主标题建议 64–80px，内容页结论标题 48–64px、最多两行，可选的眉题/小标题 18–22px；正文 24–32px，图表标签和注释不得低于 18px；不通过反复缩小文字解决溢出；
- AI 可以从材料生成第一版结论标题，但标题始终是最容易直接编辑的字段；小标题只在承担分组、时间、口径或结论层级时生成，没有信息作用就不生成；
- 图片、图表和表格在合并后执行统一基线检查。

### 根据内容自由决定的部分

- Scene 数量；
- 图表、表格、流程、关系图、图文或纯文字表达；
- 构图、视觉隐喻、信息密度和动态表达；
- 是否需要小标题。小标题只有在帮助理解层级时才生成，不能为了填模板而生成。

协调人不填写视觉要求。任务卡只由 Skill 自动盖上 `skillContractVersion + designContract`：设计 token 版本、确定性的艺术方向种子、1600×900 舞台和无页面装饰。各负责人因此共享相同的字体、色彩、基础字号、背景和动效语言；内容构图仍由各自材料决定。合并器先校验契约，再确定性统一全局 token，不把页面强制套进六类母版。

## 9. 技术实施分期

### Phase 0：可行性验证，不进入正式发布

目标：先证明最容易误判的技术边界。

- 在 Chrome/Edge 的本地单文件场景验证 `showSaveFilePicker`、再次保存、关闭重开、权限恢复和降级保存。
- 验证 5 MB、20 MB、60 MB 内嵌资产工作文件的打开、编辑、保存、重新加载和合并时间。
- 验证工作 HTML 可以直接提取内嵌包，不能依赖页面 DOM 逆向恢复。
- 验证把文件复制到空目录、改名、通过普通文件传输后，图片和编辑功能仍正常。

通过标准：结果写成支持矩阵和基准数据；任何浏览器限制必须明确暴露，不能隐藏。

### Phase 1：任务卡与 v0.12 工作文件协议

- 新增一次性任务卡 schema：报告 ID、大纲原编号、章节 ID、负责人、顺序、警告。
- 任务卡由 Skill 自动写入 Skill 契约版本、设计 token 版本、艺术方向种子、1920×1080 舞台和页面装饰策略；用户不额外配置。
- 新增 workfile manifest：版本、报告/章节身份、修订谱系、内容哈希、不可变/可变条目。
- 将任务卡生成设为显式独立命令；章节制作不得自动新建任务卡。
- 兼容读取 v0.11 任务简报和 ZIP，避免已有工作作废。

### Phase 2：单一工作 HTML 与保存

- `pack-section` 默认只交付一个 `.mint-section.html`。
- 标准包从第一版开始内嵌；保留显式 `--technical-zip` 供排障。
- 增加保存、未保存状态、直接写回和降级保存。
- 保存采用可变层增量序列化和哈希防抖。

### Phase 3：全类型编辑加固

- 所有正式内容绑定稳定 `data-field-path`；字段覆盖率 100%。
- 加固文本、表格、图表、媒体和关系图编辑器。
- 增加编辑区域宽度、完整输入、保存重开、类型化数据回读硬门。
- 明确结构性修改由 Agent revision 执行，并保留人工修改。

### Phase 4：直接合并工作 HTML

- 合并器同时接受 v0.12 工作 HTML 和兼容的 v0.11 ZIP。
- 从 HTML 读取内嵌包，验证 SHA-256、来源锁、身份和修订谱系。
- 实现改名无关、完全重复去重、严格祖先选新、分叉阻断规则。
- 保持确定性命名空间和任务卡顺序；合并期间模型调用数必须为 0。
- 确定性统一全局 token、字体、背景、16:9 舞台和动效基线；不触碰章节事实和人工修改。

### Phase 5：最终发布性能优化

- 合并浏览器启动，建立一次性 `publish-snapshot`。
- PDF 和 PPTX 校验同一内容哈希、同一当前模型和同一几何状态。
- 保持 16:9、无页面装饰、原生对象和格式一致性硬门。

### Phase 6：性能与三人协作端到端验收

模拟真实流程：

1. 一名协调人生成任务卡；
2. A/B/C 并行产出工作 HTML；
3. 文件任意改名；
4. 领导分别直接改标题、正文、表格、图表和图片；
5. 某章节由 Agent 拆一页，另一章节合并两页；
6. 保存、关闭、重开，人工修改仍在；
7. 混入重复版本和分叉版本，验证去重/阻断；
8. 合并完整 HTML，进行一次跨章节修改；
9. 发布 HTML/PPTX/PDF；
10. 将最终 HTML 移到空目录离线打开，并检查图片与编辑。

连续 3 次标准用例在 30 分钟内且所有硬门通过，才允许进入安装候选。

### Phase 7：安全安装与回滚

- 在任何修改前保存当前 v0.11 完整树和校验值，包括当前尚未提交的有效实现。
- v0.12 在独立候选目录或分支开发，不直接覆盖已安装 Skill。
- 比较完整文件树，执行合同、浏览器、协作、PPTX/PDF 和性能测试。
- 全部通过后再备份 `/Users/mac/.codex/skills/mint-report-html` 并替换。
- 安装后从已安装路径再运行关键合同和标准流程。
- 失败立即回滚 v0.11；不修改 `mint-report-deck`。

### 两天实施安排与止损线

这两天只做本方案中的核心链路，不扩展在线多人同编、云端发布等功能：

| 时间 | 工作 | 当段通过条件 |
|---|---|---|
| 第 1 天上午 | Phase 0 保存/体积验证；冻结 task/workfile 协议 | 支持矩阵明确；60 MB 工作文件可打开、改、存、重开 |
| 第 1 天下午 | 一次任务卡、单工作 HTML、直接编辑/保存、改名无关 | 单章节从生成到领导修改再重开通过浏览器 E2E |
| 第 2 天上午 | HTML 直接合并、版本冲突、全局设计契约统一 | 三章节合并为 0 模型调用，内容哈希和人工修改保持 |
| 第 2 天下午 | 统一 publish、性能回归、标准用例、备份安装 | 硬门全过且标准用例达标后才安装 |

止损线：如果第 1 天上午证明浏览器无法可靠同文件保存，立即采用“一个当前版 HTML 的降级保存”完成核心协作，不在两天内新增桌面应用或本地后台服务；如果标准用例速度未达标，保留 v0.11，不用删内容或关闭门禁换发布。

## 10. 计划修改的文件范围

最终实施时预计只触及：

| 范围 | 目的 |
|---|---|
| `SKILL.md` | 缩短入口、明确五条路径和不可牺牲硬门 |
| `references/collaboration-workflow.md` | 改为一次任务卡、单工作 HTML、异步审阅和直接合并 |
| `references/performance-workflow.md` | 加入标准用例、容量预检、分阶段时间预算和发布门禁 |
| `references/data-expression-routing.md` | 保留完整规则，增加可检索关系标签，不删规则 |
| `schemas/` | 任务卡、工作文件 manifest、修订谱系 |
| `scripts/collaboration-package.mjs` | HTML 内嵌包读取、兼容 ZIP、版本冲突和确定性合并 |
| `scripts/run-creative-workflow.mjs` | 工作流路径、增量 revision、阶段计时和统一 publish session |
| `assets/mint-fields.js` | 编辑持久化、哈希防抖、未保存状态 |
| `assets/mint-package-export.js` | 单 HTML 保存、不可变哈希复用、技术 ZIP 可选 |
| `assets/mint-typed-editor.js` | 类型化编辑和几何加固 |
| `scripts/qa-creative-html.mjs`、浏览器 QA | 全字段和编辑区几何硬门 |
| `tests/` | v0.12 合同、真实浏览器、三人协作、性能和回归测试 |

不计划增加后台服务、多人在线实时同编、云盘、权限系统或新的桌面应用。这些会扩大范围并威胁两天内完成核心问题。

## 11. 验收测试清单

### 内容与来源

- [ ] 任务卡保留原大纲编号，缺失编号只警告、不补写。
- [ ] 每个来源单元在 source-lock 和 source-ledger 中有去向。
- [ ] 0 个材料外新增事实；不确定内容明确标注。
- [ ] 拆页、并页后来源去向仍完整。
- [ ] 合并过程模型调用数为 0，内容哈希在未编辑章节保持不变。

### 编辑与保存

- [ ] 不写编辑要求也自动具备 `E` 和编辑按钮。
- [ ] 标题、正文、来源、表格、图表、图片、关系图全部真实编辑成功。
- [ ] 编辑框宽度和可达内容通过几何硬门。
- [ ] 保存、关闭、重开后全部人工修改仍在。
- [ ] 浏览器不支持直接保存时，只产生一个包含修改的 HTML。
- [ ] 结构性 Agent revision 保留此前人工修改。

### 文件、图片与合并

- [ ] 工作文件改名后仍可识别和合并。
- [ ] 同内容重复文件自动去重。
- [ ] 严格祖先版本选新并记录。
- [ ] 同 revision 不同内容和分叉版本阻断。
- [ ] 文件移到空目录/另一离线环境后图片、字体和编辑仍正常。
- [ ] 外链、绝对路径、项目外素材不能通过交付门禁。
- [ ] A/B/C 合并后顺序按任务卡，Scene ID、CSS、图片 ID 无碰撞。
- [ ] A/B/C 使用同一自动设计契约；Skill 版本或契约版本不一致时阻断并提示升级。

### 视觉与格式

- [ ] 简洁清新品牌锚点一致，但 Scene 构图不被固定模板限制。
- [x] 每个桌面 Scene 舞台为 1920×1080（16:9），比例误差不超过 0.5%。
- [ ] 个人、合并和最终阶段都无页眉、页脚、页码。
- [ ] 最终 PPTX 16:9，所有内容为原生可编辑对象。
- [ ] 最终 HTML、PPTX、PDF 的标题、数据、来源和顺序一致。
- [ ] desktop/laptop/mobile/print 碰撞、溢出和标题断行检查通过。

### 性能

- [ ] 20 秒内完成输入容量预检。
- [ ] 未变 Office/PDF 规范化次数为 0。
- [ ] 普通 revision 只编译受影响 Scene；无变化直接退出。
- [ ] 保存只重算可变条目哈希。
- [ ] 最终发布仅启动一次浏览器会话。
- [ ] 标准用例连续 3 次端到端不超过 30 分钟。
- [ ] v0.11 的来源、编辑、路由、离线、PPTX/PDF 和视觉门禁无一回退。

## 12. 性能报告必须让超时可定位

当前性能报告主要覆盖脚本阶段。v0.12 需要记录完整墙钟链路：

```json
{
  "capacity": {"files": 18, "pages": 72, "scanPages": 0, "assetsMB": 31, "scenes": 10},
  "timingsMs": {
    "preflight": 1200,
    "normalize": 38000,
    "contentCompile": 21000,
    "sceneBatchAuthor": 310000,
    "assemble": 4100,
    "desktopQa": 12000,
    "saveWorkfile": 950,
    "merge": 1800,
    "finalQa": 28000,
    "pptx": 9000,
    "pdf": 4500
  },
  "reuse": {"normalizedAssets": 18, "reusedScenes": 8, "compiledScenes": 2},
  "modelCalls": {"contentCompile": 1, "sceneBatch": 1, "merge": 0},
  "gateResult": "pass"
}
```

如果超时，可以明确是扫描/OCR、模型创作、浏览器 QA、保存体积、PPTX 还是 PDF，而不是笼统说“Agent 很慢”。

## 13. 评审时需要确认的五项决定

以下是方案真正需要业务侧确认的内容，确认后才执行：

1. **任务卡**：由指定协调人单独生成一次，A/B/C 不自动各自创建。
2. **日常文件**：每人只管理一个 `.mint-section.html`；ZIP 默认隐藏为内部技术格式。
3. **支持浏览器**：Chrome/Edge 作为同文件保存的正式目标；其他浏览器允许一次 HTML 降级保存。
4. **版本冲突**：只对严格祖先关系自动选新，任何分叉一律阻断并让人选择。
5. **30 分钟口径**：接受第 7.5 节标准用例边界；超出边界先容量预警，不以丢内容换时间。

## 14. 执行前后的交付物

方案批准后，实施交付必须包含：

- v0.12 Skill 候选完整树；
- 当前 v0.11 时间戳备份和完整树对比；
- 用户操作手册，按“协调人 / 负责人 / 领导 / 最终发布”四个角色编写；
- 一套任务卡、A/B/C 工作 HTML、合并 review.html、最终 PPTX/PDF 示例；
- 全量合同和浏览器测试结果；
- 三次 30 分钟标准用例性能报告；
- 已知浏览器限制和降级路径；
- 安装验证与一键回滚说明。

只有上述交付齐全，且速度和能力回归门禁同时通过，才把 v0.12 作为默认 `mint-report-html` 使用。
